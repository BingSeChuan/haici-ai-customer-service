# 运行指南

本指南描述如何在本地完整运行本项目（MySQL + 后端 + 前端）。全程约 10 分钟（不含模型下载时间）。

## 环境要求

| 依赖 | 版本 | 说明 |
|------|------|------|
| Docker | 20+ | 运行 MySQL 8 |
| Python | 3.10+ | 后端 |
| Node.js | 18+ | 前端 |

## 第 1 步：启动 MySQL

```bash
cd backend
docker compose up -d
```

首次会拉取镜像并创建数据库 `haici_cs`（账号 `haici/haici_2026`）。国内网络拉取失败时可用镜像源：

```bash
docker pull docker.m.daocloud.io/library/mysql:8.0 && docker tag docker.m.daocloud.io/library/mysql:8.0 mysql:8.0
docker compose up -d
```

验证：`docker ps` 中 `haici_mysql` 状态为 `healthy`。

## 第 2 步：安装后端依赖

```bash
cd backend
python -m venv .venv
.venv/Scripts/pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> 中国大陆网络建议使用清华镜像加速；`pip install` 会同时安装 torch（CPU 版）与 sentence-transformers，约 1-2 GB。

## 第 3 步：配置环境变量

```bash
cd backend
cp .env.example .env
# 编辑 .env，填入 DeepSeek API Key：
#   LLM_API_KEY=sk-xxxxxxxxxxxxxxxx
```

所有密钥只存在于本地 `.env`（已被 .gitignore 排除，不会提交）。

**没有 API Key 怎么办？**（题目 FAQ 允许的替代方案）
- 把 `.env` 中 `LLM_BASE_URL` / `LLM_MODEL` / `LLM_API_KEY` 换成任意 OpenAI 兼容端点（通义千问 / 月之暗面 / Groq 均有免费额度）；
- 或用本地 Ollama：`LLM_BASE_URL=http://localhost:11434/v1`、`LLM_MODEL=llama3.1`（无需 Key，填入任意字符串即可），仅需保证 Ollama 已启动对应模型。

**Embedding 切换**（默认本地 bge-small-zh-v1.5，首次运行自动从 hf-mirror.com 下载，约 100MB）：
- 中国大陆网络默认走 `HF_ENDPOINT=https://hf-mirror.com` 镜像，无需手动操作；
- 如需切换为 API 型 embedding：`.env` 中设 `EMBEDDING_PROVIDER=openai_compatible`，并填 `EMBEDDING_API_KEY/BASE_URL/MODEL`（如 SiliconFlow 免费 bge-m3）。

## 第 4 步：初始化数据库

```bash
cd backend
.venv/Scripts/python scripts/init_db.py
```

创建全部表 + 管理员账号：**13800000000**（is_admin=true，可看管理后台）。管理员密码来自环境变量 `ADMIN_PASSWORD`（`init_db.py` 未设置该变量会拒绝创建并退出）：

```bash
# Windows PowerShell
$env:ADMIN_PASSWORD = "你的强密码"    # 演示环境可用 admin123
# Linux/macOS
export ADMIN_PASSWORD="你的强密码"
.venv/Scripts/python scripts/init_db.py
```

## 第 5 步：启动后端

```bash
cd backend
.venv/Scripts/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000
```

启动时会自动完成两件事：
1. 首次运行下载 embedding 模型（约 1-3 分钟，取决于网络）；
2. 将 `data/seed_docs/` 下的 3 篇示例文档（产品介绍 / FAQ / 退换货政策）自动解析、分块、向量化（幂等，已处理则跳过）。

看到 `Uvicorn running on http://0.0.0.0:8000` 后，访问 http://localhost:8000/docs 可查看 Swagger 接口文档。

## 第 6 步：启动前端

```bash
cd frontend
npm install
npm run dev
```

浏览器访问 http://localhost:5173 。

## 第 7 步：快速验证

| 操作 | 预期 |
|------|------|
| 用 13800000000 / 初始化时设置的 ADMIN_PASSWORD 登录 | 进入系统，左侧为历史会话 |
| 问"文博ERP标准版多少钱一年？" | 逐字流式输出"3999元/年"，下方出现来源卡片与追问建议 |
| 问"今天上海天气怎么样？" | 返回兜底话术（不编造） |
| 问"软件不想要了能退款吗？" | 分情况回答，标注引用编号 [1][3][4][6][7] |
| 点击"知识库" | 3 篇示例文档状态为"已就绪" |
| 点击"管理后台" | 统计卡片、反馈分布、近 7 日问答量折线图 |

自动化冒烟测试（可选）：

```bash
cd backend
PYTHONIOENCODING=utf-8 .venv/Scripts/python scripts/smoke_test.py
```

## 数据迁移（升级注意事项）

**遗忘机制修复后的存量记忆回填**：若从旧版本升级（`vector_id` 列引入前写入的记忆），需运行一次回填脚本，否则存量记忆不会被检索（静默失效）：

```bash
cd backend
.venv/Scripts/python scripts/backfill_memory_vector_id.py
```

脚本用 Chroma 向量 metadata 按 `(user_id, session_id, content)` 精确匹配回填 MySQL 行的 `vector_id`，幂等可重复执行。

## 常见问题

**Q：MySQL 起不来 / 端口被占？**
改 `docker-compose.yml` 的端口映射（如 `3307:3306`），并同步修改 `.env` 的 `MYSQL_PORT`。

**Q：embedding 模型下载失败？**
检查网络后重试；或切换为 `EMBEDDING_PROVIDER=openai_compatible` 使用 API embedding（见第 3 步）。

**Q：问答返回"未配置 LLM_API_KEY"？**
检查 `.env` 是否已填 Key、是否重启了后端。

**Q：回答质量相关参数怎么调？**
`.env` 中的 `RAG_TOP_K`（片段数）、`RAG_SIMILARITY_THRESHOLD`（阈值，调高更严格）、`RAG_CONTEXT_BUDGET_CHARS`（上下文预算）、`CHUNK_SIZE`（分块大小）均可在不改代码的情况下调整。
